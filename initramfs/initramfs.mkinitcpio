#!/bin/bash

build() {
  . /etc/mkinitramfs.conf

  for KERNEL in ${INITRAMFS_KERNEL}; do
    mkinitramfs "${KERNEL}"
  done
}

help() {
  cat <<HELPEOF
This hook generates initramfs for initsystem.
HELPEOF
}
