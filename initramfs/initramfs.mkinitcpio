#!/bin/bash

build() {
  for PRESET in /etc/mkinitcpio.d/*.preset; do
    mkinitramfs `basename "${PRESET}" .preset`
  done
}

help() {
  cat <<HELPEOF
This hook generates an initramfs for the initsystem.
HELPEOF
}
